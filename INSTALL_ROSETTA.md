# 🔧 تثبيت Rosetta - حل مشكلة Architecture

## ⚠️ المشكلة:

Flutter يحتاج **Rosetta** على Apple Silicon Macs (M1/M2/M3).

---

## 🚀 الحل - خطوة واحدة:

افتح **Terminal** (أو استخدم Terminal هنا) واكتب:

```bash
sudo softwareupdate --install-rosetta --agree-to-license
```

ثم:
1. أدخل **كلمة مرور macOS** (كلمة مرور تسجيل الدخول)
2. انتظر حتى ينتهي التثبيت (حوالى دقيقة)

---

## ✅ بعد التثبيت:

ارجع واكتب:

```bash
cd /Users/amrhamdy/Documents/Projects/wish_listy
flutter run -d 00008030-001D18AA14DB802E
```

أو استخدم **Xcode** - اضغط **▶️**

---

## 📝 ملاحظة:

Rosetta ده برنامج من Apple بيسمح للتطبيقات القديمة (x86) بالعمل على Apple Silicon. Flutter محتاج ده للـ iOS development tools.

**جرب الأمر الأول وأخبرني! 🎯**

